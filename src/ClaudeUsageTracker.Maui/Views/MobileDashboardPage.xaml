<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClaudeUsageTracker.Maui.Views.MobileDashboardPage"
             Title="Claude Usage">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">

            <Label Text="Claude Usage" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" />

            <!-- Today card -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="12" Padding="20">
                <VerticalStackLayout>
                    <Label Text="Today" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding TodayCostUsd, StringFormat='{0:C}'}" FontSize="32" FontAttributes="Bold" />
                    <Label Text="{Binding TodayTotalTokens, StringFormat='{0:N0} tokens'}" FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                </VerticalStackLayout>
            </Frame>

            <!-- This month card -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="12" Padding="20">
                <VerticalStackLayout>
                    <Label Text="This Month" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding MonthCostUsd, StringFormat='{0:C}'}" FontSize="32" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Frame>

            <!-- Top model -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="12" Padding="20"
                   IsVisible="{Binding HasTopModel}">
                <VerticalStackLayout>
                    <Label Text="Top Model" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding TopModelName}" FontSize="16" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Frame>

            <!-- Last updated -->
            <Label Text="{Binding LastUpdated, StringFormat='Last updated: {0}'}"
                   FontSize="12"
                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                   HorizontalOptions="Center" />

            <Button Text="Refresh"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsRefreshing, Converter={StaticResource InvertedBoolConverter}}"
                    HorizontalOptions="Fill" />

            <ActivityIndicator IsRunning="{Binding IsRefreshing}"
                               IsVisible="{Binding IsRefreshing}"
                               HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
