<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:ClaudeUsageTracker.Maui.Views"
             x:Class="ClaudeUsageTracker.Maui.Views.DashboardPage"
             Title="Claude Usage Tracker">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto" Padding="20" RowSpacing="16">

        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto">
            <Label Text="Claude Usage Tracker" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" />
            <Button Grid.Column="1" Text="Settings"
                    Clicked="OnSettingsClicked"
                    BackgroundColor="Transparent"
                    TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                    Margin="0,0,8,0" />
            <Button Grid.Column="2" Text="Refresh"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsRefreshing, Converter={StaticResource InvertedBoolConverter}}" />
        </Grid>

        <!-- Stats Cards — billing (Admin API) -->
        <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*" ColumnSpacing="12" IsVisible="{Binding HasAdminApiData}">
            <Frame Grid.Column="0" BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="8" Padding="16">
                <VerticalStackLayout>
                    <Label Text="Today" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding TodayCostUsd, StringFormat='{0:C}'}" FontSize="24" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Frame>
            <Frame Grid.Column="1" BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="8" Padding="16">
                <VerticalStackLayout>
                    <Label Text="This Week" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding WeekCostUsd, StringFormat='{0:C}'}" FontSize="24" FontAttributes="Bold" />
                    <Label Text="{Binding WeekTotalTokens, StringFormat='{0:N0} tok'}" FontSize="11"
                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                </VerticalStackLayout>
            </Frame>
            <Frame Grid.Column="2" BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="8" Padding="16">
                <VerticalStackLayout>
                    <Label Text="This Month" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding MonthCostUsd, StringFormat='{0:C}'}" FontSize="24" FontAttributes="Bold" />
                </VerticalStackLayout>
            </Frame>
            <Frame Grid.Column="3" BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}" CornerRadius="8" Padding="16">
                <VerticalStackLayout>
                    <Label Text="Last Updated" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    <Label Text="{Binding LastUpdated}" FontSize="16" />
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <!-- Daily Cost Chart -->
        <Grid Grid.Row="2" RowDefinitions="Auto,*">
            <Label Grid.Row="0" Text="Daily Cost — Last 30 Days" FontAttributes="Bold" Margin="0,0,0,8" />
            <GraphicsView Grid.Row="1" x:Name="CostChart" HeightRequest="200" />
        </Grid>

        <!-- Token Usage Chart -->
        <Grid Grid.Row="3" RowDefinitions="Auto,Auto,Auto,*">
            <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,8">
                <Label Text="Token Usage" FontAttributes="Bold" VerticalOptions="Center" />
                <Picker Grid.Column="1"
                        x:Name="ProviderPicker"
                        Title="Provider"
                        SelectedIndexChanged="OnProviderChanged"
                        WidthRequest="140">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Anthropic</x:String>
                            <x:String>MiniMaxi</x:String>
                            <x:String>GoogleAI</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </Grid>

            <StackLayout Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Text="24h" Command="{Binding SetTimeRangeCommand}" CommandParameter="Past24Hours" Padding="8,4" />
                <Button Text="7d" Command="{Binding SetTimeRangeCommand}" CommandParameter="Past7Days" Padding="8,4" Margin="4,0" />
                <Button Text="30d" Command="{Binding SetTimeRangeCommand}" CommandParameter="Past30Days" Padding="8,4" />
            </StackLayout>

            <GraphicsView Grid.Row="2" x:Name="TokenChart" HeightRequest="200" />
        </Grid>

    </Grid>
</ContentPage>
