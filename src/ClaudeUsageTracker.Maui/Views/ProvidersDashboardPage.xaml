<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClaudeUsageTracker.Maui.Views.ProvidersDashboardPage"
             Title="Providers">

    <Grid RowDefinitions="Auto,*,Auto" Padding="20" RowSpacing="16">

        <!-- Header -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
            <Label Text="Plan Usage" FontSize="22" FontAttributes="Bold" VerticalOptions="Center" />
            <Button Grid.Column="1" Text="Refresh"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsRefreshing, Converter={StaticResource InvertedBoolConverter}}" />
        </Grid>

        <!-- Provider cards -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="12">
                <VerticalStackLayout.Triggers>
                    <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding Providers.Count}" Value="0">
                        <Setter Property="IsVisible" Value="False" />
                    </DataTrigger>
                </VerticalStackLayout.Triggers>

                <CollectionView ItemsSource="{Binding Providers}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame CornerRadius="10" Padding="20"
                                   BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}">
                                <VerticalStackLayout Spacing="12">
                                    <!-- Provider name -->
                                    <Label Text="{Binding ProviderName}" FontSize="18" FontAttributes="Bold" />

                                    <!-- Interval + Weekly in a grid -->
                                    <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                                        <VerticalStackLayout Grid.Column="0">
                                            <Label Text="Session" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                            <Label Text="{Binding IntervalUtilization, StringFormat='{0}% used'}"
                                                   FontSize="20" FontAttributes="Bold" />
                                            <Label Text="{Binding IntervalUsed, StringFormat='{0:N0} / {1:N0}'}" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                            <Label Text="{Binding IntervalResetsAt}" FontSize="11"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                        </VerticalStackLayout>
                                        <VerticalStackLayout Grid.Column="1">
                                            <Label Text="Weekly" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                            <Label Text="{Binding WeeklyUtilization, StringFormat='{0}% used'}"
                                                   FontSize="20" FontAttributes="Bold" />
                                            <Label Text="{Binding WeeklyUsed, StringFormat='{0:N0} / {1:N0}'}" FontSize="12"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                            <Label Text="{Binding WeeklyResetsAt}" FontSize="11"
                                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                                        </VerticalStackLayout>
                                    </Grid>

                                    <!-- Last updated -->
                                    <Label Text="{Binding LastUpdated, StringFormat='Updated {0}'}" FontSize="11"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}" />
                                </VerticalStackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Empty state -->
        <VerticalStackLayout Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Center">
            <VerticalStackLayout.Triggers>
                <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding Providers.Count}" Value="0">
                    <Setter Property="IsVisible" Value="True" />
                </DataTrigger>
            </VerticalStackLayout.Triggers>
            <Label Text="No providers connected" FontSize="16" HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}" />
            <Label Text="Add API keys in Settings to see usage here" FontSize="13" HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}" />
        </VerticalStackLayout>

        <!-- Error -->
        <Label Grid.Row="2" Text="{Binding ErrorMessage}" TextColor="Red" FontSize="12"
               IsVisible="{Binding HasError}" />

        <ActivityIndicator Grid.Row="2" IsRunning="{Binding IsRefreshing}" IsVisible="{Binding IsRefreshing}"
                          HorizontalOptions="Center" />

    </Grid>
</ContentPage>
